# Architecture — Landing Page de Serviços

> Fonte da verdade sobre **como** o site é construído.
> Conteúdo e requisitos: ver `prd.md`. Convenções de código: ver `CLAUDE.md`.

## 1. Stack

- **React 18 + Vite + TypeScript** — SPA simples, sem SSR (não há necessidade de SEO multi-página numa landing one-page).
- **mothership-ds** — biblioteca de componentes padrão do projeto (github.com/valnezjr/mothership-ds, do próprio autor). Instalada como dependência git presa a uma tag (`"mothership-ds": "github:valnezjr/mothership-ds#v1.3.0"`), não à branch `main` — a lib está em desenvolvimento ativo em paralelo, então a versão só avança quando alguém bumpar a tag de propósito. Não publicada no npm; sem build próprio (`main`/`types` apontam pro `.tsx` fonte direto), o Vite transforma normalmente via esbuild, sem config extra.
- **Tailwind CSS** — só layout/posicionamento (flex, grid, espaçamento). Cor e tipografia são proibidas aqui: vêm inteiramente do mothership-ds (`var(--color-*)`, `.ms-h1`/`.ms-text-sm`/etc.), herdadas via `.ms-page` aplicada no `#root` (`index.html`).
- **lucide-react** — ícones (mothership-ds não embute biblioteca de ícones, por design).
- **motion (framer-motion)** — [ou "CSS transitions puras"] para o fade entre seções. Decisão registrada na etapa de transições.

## 2. Estrutura de pastas

```
src/
├── App.tsx              # Shell: estado de navegação + layout raiz + <ThemeProvider>
├── index.css            # Tailwind (só layout) + import do mothership-ds + regra de no-scroll
├── main.tsx             # importa index.css e mothership-ds/styles.css uma única vez
├── components/
│   ├── Navbar.tsx       # Navbar flutuante (desktop) + menu mobile na Etapa 8 (Drawer do
│   │                     mothership-ds) — reaproveita classes .ms-navbar* do mothership-ds,
│   │                     navegação por estado (não por href)
│   └── SectionShell.tsx # Wrapper comum: padding-top p/ navbar, fade, 100% da área útil
├── sections/
│   ├── Home.tsx
│   ├── Servicos.tsx
│   ├── Sobre.tsx
│   └── Contato.tsx
└── lib/
    ├── navigation.ts    # SectionId, ordem canônica das seções
    ├── utils.ts         # cn() (clsx + tailwind-merge) — só pra classes de layout do Tailwind
    └── content.ts       # [opcional] textos centralizados, espelhando o prd.md
```

Não há mais `components/ui/` — o catálogo de componentes vem do mothership-ds
(instalado em `node_modules/mothership-ds`), não de arquivos gerados no repo.

## 3. Navegação — decisão central

**Sem react-router.** A navegação é por estado:

```ts
type SectionId = "home" | "servicos" | "sobre" | "contato";
// App mantém: const [active, setActive] = useState<SectionId>("home")
```

- Uma seção montada por vez, renderizada sob a navbar.
- **Por quê:** não há URLs distintas a preservar, não há SSR, e rotas reais criariam
  complexidade (scroll restoration, history) sem benefício numa página única.
- Trade-off aceito: sem deep-linking por URL. [Se quiser deep-link depois:
  sincronizar `active` com `location.hash` — mudança pequena e isolada no App.]
- Ordem canônica das seções (para navegação por setas): home → servicos → sobre → contato.

## 4. Layout sem scroll — regras invioláveis

1. Container raiz: `100dvw` x `100dvh` (**dvh, nunca vh** — barra de endereço mobile), `overflow: hidden` em html/body/#root.
2. Navbar: `position: fixed`, flutuante (margens, cantos arredondados, backdrop-blur), sobrepõe o conteúdo.
3. Toda seção usa o `SectionShell`, que já aplica o padding-top compensando a navbar — seções nunca calculam isso individualmente.
4. Se o conteúdo não cabe: adaptar o conteúdo (carrossel, Modal, corte de texto). **Nunca** criar scroll interno ou reduzir fonte abaixo do legível.

## 5. Transições

- Fade-out da seção atual → troca → fade-in da nova (total 400–500ms, ease-in-out, com deslocamento sutil de ~8px no fade-in).
- Navegação bloqueada durante a transição (evita estados intermediários quebrados).
- `prefers-reduced-motion`: troca instantânea.

## 6. Tema e styleguide

- O styleguide **é** o mothership-ds — não é mais um tema aplicado depois por cima de outra biblioteca, é a própria biblioteca de componentes do projeto (decisão #3 abaixo, substitui a original de shadcn/ui + tokens semânticos).
- Todo estilo de cor/tipografia passa pelas CSS variables do mothership-ds (`var(--color-accent)`, `var(--color-text)`, `var(--font-family)`, etc., ver `node_modules/mothership-ds/src/styles/tokens.css`), herdadas via `.ms-page` no `#root`. Tipografia usa as classes utilitárias da lib (`.ms-h1`, `.ms-h2`, `.ms-h3`, `.ms-text-sm`, `.ms-text-xs`, `.ms-text-muted`).
- Tailwind é só pra layout (flex, grid, gap, padding, largura/altura) — nunca pra cor, fonte ou raio de borda.
- Se um dia divergir do tema padrão do mothership-ds só pra este projeto: não editar `node_modules` (some no próximo install); redeclarar as `--color-*`/`--font-family` que quiser depois dos imports em `index.css`.

## 7. Contato

- Sem formulário — só links diretos (WhatsApp, e-mail, Instagram). Decisão de
  2026-08-05: um campo de nome/e-mail/mensagem não fazia sentido pra esta
  seção específica (não é uma remoção do `Field`/`Input`/`Textarea`/`Button`
  da lib mothership-ds, que seguem disponíveis pra qualquer outro uso).
  `lib/sendContact.ts` (a função de envio isolada da v1, pensada pra plugar
  Formspree/EmailJS/API depois) foi removido junto — ficou órfão sem o
  formulário que o chamava.

## 8. Registro de decisões

| # | Decisão | Motivo | Data |
|---|---|---|---|
| 1 | SPA sem router, navegação por estado | Página única, sem necessidade de URLs | [data] |
| 2 | dvh em vez de vh | Barra de endereço mobile quebra 100vh | [data] |
| 3 | mothership-ds como biblioteca de componentes padrão, substituindo shadcn/ui por completo | Biblioteca própria do autor (github.com/valnezjr/mothership-ds); "styleguide futuro" deixou de ser um tema a aplicar depois e passou a ser a própria lib de componentes, usada desde já | 2026-08-05 |
| 4 | Dependência git presa (nunca à branch `master`, que segue em desenvolvimento ativo em paralelo) — a uma tag pra mudanças que valem uma versão de verdade, ou a um commit SHA específico pra correções pequenas feitas sob demanda | mothership-ds estava ganhando uma tag `vX.Y.0` a cada pedido pontual (v1.5→v1.6→v1.7 em poucos dias) — versionamento demais, sem relação com o roadmap real da lib (README.md § Roadmap do próprio repositório mothership-ds). Fixar por commit SHA dá a mesma reprodutibilidade de uma tag sem forçar um número de versão a cada ajuste; tag continua reservada pra quando a mudança realmente merece virar uma versão publicada | 2026-08-06 |
| 5 | Navbar do projeto é local (não usa `<Navbar>` do mothership-ds), reaproveitando as classes `.ms-navbar*` | O `<Navbar>` da lib espera links reais (`href`) com scroll-spy; aqui a navegação é por estado/clique, sem URLs (decisão #1) | 2026-08-05 |
| 6 | Transição de fade só com CSS (`@keyframes` + estado do React), sem framer-motion/motion | É um fade sequencial simples (sai → monta a nova → entra) com um leve deslocamento vertical — não precisa de gestos, layout compartilhado (`layoutId`) nem spring physics que justifiquem trazer uma lib nova só pra isso; `useSectionTransition` (`src/lib/`) orquestra sai/monta/entra com dois `setTimeout` casados às durações do CSS | 2026-08-05 |
| 7 | Serviços empilha no mobile (não vira carrossel) | Só 3 itens curtos (ícone + título + 1–2 linhas) — um `Carousel` seria mecanismo demais pra tão pouco conteúdo; empilhado cabe sem scroll até em viewports baixas (testado 360×740 e 1366×600) | 2026-08-05 |
| 8 | Preview de desenvolvimento no GitHub Pages (repo `valnezjr/valnezjrlp`, público — obrigatório pro Pages gratuito), não Vercel/Netlify como o roteiro sugeria | Deploy automático a cada push em `main` via Actions (`.github/workflows/pages.yml`), sem custo/config extra, suficiente pra acompanhar progresso; não é a hospedagem final — sem lib de rotas nem URLs profundas (decisão #1), não há armadilha de SPA fallback pra configurar no Pages | 2026-08-05 |
| 9 | `Modal` do mothership-ds ganha a prop `headerExtra` (upstream, v1.4.0) | `.ms-modal__body` tem `overflow-x: hidden` (reserva de espaço da barra de rolagem) — cortava o hover de ícones com `HoverEdge` no cabeçalho dos modais de Serviços; `.ms-modal__head` não tem essa restrição. Mudança feita na própria lib, não um workaround local | 2026-08-05 |
| 10 | Seção Contato sem formulário — só links diretos (WhatsApp, e-mail, Instagram) | Campo de nome/e-mail/mensagem não fazia sentido pra esta seção específica; `lib/sendContact.ts` removido (ficou órfão). Não é remoção de `Field`/`Input`/`Textarea`/`Button` da lib mothership-ds, que seguem disponíveis | 2026-08-05 |
| 11 | Menu mobile da Navbar usa `Drawer` (`side="right"`), não `.ms-navbar__menu` (o dropdown que o `<Navbar>` do mothership-ds usa por padrão) | Mesmo mecanismo que a `Sidebar` já usa pra própria gaveta mobile (decisão #9) — bem mais simples de reaproveitar (props prontas) do que replicar a lógica de posicionamento/portal do `.ms-navbar__menu`; `side="right"` porque o botão hamburguer cai à direita na `.ms-navbar` custom deste projeto. `.ms-navbar:has(.ms-navbar__burger)` já esconde os links diretos e mostra o hamburguer abaixo de 720px — CSS pronto da lib, nenhuma regra nova precisou ser escrita aqui | 2026-08-05 |
| 12 | Splash de tela cheia (`App.tsx`, `loading` state) + `Splash inline persistent ready` junto do Hero (`Home.tsx`) como visual de apoio, resolvendo o campo que ficava em aberto no PRD | `LogoMark` não tem uso isolado fora do `Splash` por design da própria lib (partes nascem fora de posição, esperando a montagem) — `inline`+`persistent`+`ready` fixo monta e mantém o logo com o degradê de marca girando indefinidamente (`animateTransform repeatCount="indefinite"` do próprio SVG), sem repetir a sequência de entrada já vista no Splash de tela cheia. `style={{ background: "none" }}` remove o `--bg-base`/`--bg-page` que o `.ms-splash` sempre pinta por baixo (faz sentido em tela cheia, mas aqui criava uma segunda instância de fundo dentro de uma caixa visível) — sem ele, só o SVG do `LogoMark` (sem fundo próprio) sobrepõe o `LivingBackground` real da página, flutuando ao lado do Hero sem moldura nem emenda; corrigido de 256px pra 384px (`h-96 w-96`, só ≥1024px) depois de feedback direto de que a versão inicial, com borda + `var(--radius-md)`, ficava pequena e "engaiolada". Visibilidade abaixo de 1024px corrigida pela decisão #16 | 2026-08-05 |
| 13 | `Home.tsx` passa `instant` (upstream, `Splash` v1.5.0) pro `Splash` inline a partir da segunda montagem | Achado real após a decisão #12 ir ao ar: `Home` desmonta por completo toda vez que a navegação sai da seção (decisão #1) — o `Splash` remontava do zero a cada volta, e o CSS de revelação reagia à classe `.ms-splash--ready` aparecendo num elemento novo como se fosse a primeira vez, repetindo a animação inteira (o nome "Mothership" reaparecendo centralizado no dirigível antes de assentar). `instant` (mudança na própria lib) pula a choreo e mostra a composição final direto. Controlado por uma variável de módulo em `Home.tsx` (`hasPlayedHomeIntro`) — sobrevive a remontagens do componente porque não é estado do React, só reseta com reload real da página | 2026-08-05 |
| 14 | Marca da Navbar (`.ms-navbar__brand`) trocada do texto "valnezjr" pra `LogoMark part="word"` (upstream, `LogoMark` v1.5.0) | Pedido direto — usar o nome da própria marca do design system (mothership) como identidade visual da navbar, não um texto solto. `part="word"` (mudança na própria lib) resolve o problema de que `LogoMark` só funcionava dentro do `Splash` por design: renderiza só o nome (sem o dirigível), `viewBox` recortado, já assentado numa posição final estática via `.ms-logo--word`, sem depender de `.ms-splash--ready` | 2026-08-05 |
| 15 | Logo em loop (decisão #12) visível em toda largura, não só ≥1024px — caixa `aspect-[52/35]` (mesma proporção do `viewBox` do `LogoMark`, 7800×5250 simplificado) com largura em degraus por **altura** de viewport (`w-64` padrão, `[@media(min-height:650px)]:w-72`, `:700px]:w-80`, `:850px]:w-96`) e acima do Hero (`order-first` na coluna `flex-col` do mobile) abaixo de 1024px; a partir daí volta ao tamanho e posição da decisão #12 (`lg:!aspect-auto lg:!h-96 lg:!w-96`, `lg:order-none` restaura a ordem do DOM — Hero primeiro — que em `lg:flex-row` põe a logo à direita) | Feedback direto (quatro rodadas): primeiro a logo sumia por completo no mobile; corrigido pra `h-28`/`sm:h-36` (caixa quadrada), mas ainda pequena demais — aumentada pra `h-44`/`sm:h-56`, mas o problema real não era só tamanho: uma caixa quadrada pra um desenho ~1,49:1 (bem mais largo que alto) sempre deixa bastante espaço morto em cima/embaixo (letterbox), por maior que a caixa fique. `aspect-[52/35]` resolveu o letterbox, mas ainda "pequena" — o espaço vertical realmente disponível varia MUITO por altura de tela (iPhone SE a 667px de altura tem bem menos folga que um Android a 740px), então um tamanho único por largura deixava dinheiro na mesa nas telas mais altas; graus por altura (media query, não breakpoint padrão do Tailwind, que só cobre largura) aproveitam a folga real de cada caso. `lg:!...` (`!important`) virou necessário aqui: as media queries arbitrárias de altura e o `lg:` do Tailwind têm a mesma especificidade CSS, e sem forçar a prioridade do `lg:` a ordem de saída do Tailwind podia deixar a versão de altura "vencer" por engano em telas que batem os dois critérios (ex.: 1024×768) | 2026-08-05 |
| 16 | `Hero` (`Home.tsx`) ganha `className="!p-0 lg:!px-6 lg:!pt-[84px] lg:!pb-14"`, zerando o padding vertical próprio do componente no mobile | Achado ao reverificar a decisão #15 com uma checagem melhor (posição real dos elementos vs. navbar, não só `scrollHeight`): `.ms-hero` (mothership-ds) já vem com ~140px de padding vertical próprio (pensado pra uso como hero de tela cheia sozinho); somado ao padding do `SectionShell` (que já reserva espaço pra navbar), o conteúdo de `Home` ficava mais alto que a viewport em 360×740, 375×667 (iPhone SE) e 360×640 — o texto ia atrás da navbar ou cortado embaixo, violando o critério de aceite do prd.md §7, sem nenhum scroll aparecer (por isso a decisão #15 não pegou: `overflow:hidden` do `SectionShell` escondia o excesso ao centralizar em vez de virar barra de rolagem). Zerado no mobile porque o `SectionShell` já reserva o espaço certo sozinho; restaurado ao valor original (`84px`/`56px`/`24px`, os mesmos `--space-7*1.5`/`--space-7`/`--space-5` do CSS da lib) só a partir de lg, onde havia sobra. `!important` necessário porque `mothership-ds/styles.css` importa depois do Tailwind em `main.tsx` | 2026-08-05 |
| 17 | Portfólio da seção Sobre é estático (não busca do GitHub) | Curadoria e copy controlados por Valnez, sem estado de loading/erro de rede a encaixar numa transição de fade instantânea, sem risco de rate limit de API sem autenticação num GitHub Pages público (60 req/h por IP) | 2026-08-06 |
| 18 | ~~Portfólio em galeria paginada com `StepIndicator` solto, reimplementando a grade do zero em vez do componente `Gallery` da lib~~ — **corrigido pela decisão #22**, ver lá | `Gallery` é um grid que cresce em **altura** (`grid-template-columns: repeat(auto-fill, minmax(220px,1fr))`, sem paginação nem scroll próprios), incompatível com a regra de nunca criar scroll interno (§4). A ideia de paginar em vez de rolar estava certa; a execução original (reconstruir uma grade parecida, com `Card` cru e sem filtro/badge/cor de categoria) não deveria ter abandonado o componente real. `StepIndicator` (extraído do `StepModal`, upstream v1.6.0) continua correto e é a peça de paginação usada — só passou a viver **dentro** do `Gallery` de verdade (decisão #22), não ao lado de uma grade reimplementada | 2026-08-06 |
| 19 | Sobre: apresentação (`Avatar` + texto) no topo, galeria abaixo — não lado a lado como o PRD original desenhava | Pedido direto; também ajuda com a restrição de altura baixa (§4, ~600px úteis) — dois blocos empilhados (apresentação, depois a grade paginada) usam a largura toda disponível pra grade, em vez de dividi-la ao meio com uma coluna lado a lado | 2026-08-06 |
| 20 | Itens por página da galeria calculados via `matchMedia` em JS (`useItemsPerPage`, `Sobre.tsx`), não só classes Tailwind responsivas | Paginação de verdade precisa saber quantos itens cabem por página pra calcular `totalPages` corretamente — CSS sozinho poderia esconder itens visualmente por breakpoint, mas não reduziria a contagem real de páginas do `StepIndicator`/`Gallery`. Números corrigidos pela decisão #22 (sempre 1 linha; 2/3/5 colunas, não 4/6 em 2 linhas) | 2026-08-06 |
| 21 | ~~Cards da galeria com `Card` do mothership-ds e `style` sobrepondo padding/foto pra caber numa viewport de 600px de altura~~ — **revertido pela decisão #22** | O `Card` cru nunca teve nada da identidade visual da `Gallery` (sem filtro, badge ou cor de categoria) — feedback direto apontou que o resultado "não se parecia em nada" com o componente real do design system. A solução certa pro problema de altura não era trocar de componente, era dar paginação de verdade pro `Gallery` (decisão #22) | 2026-08-06 |
| 22 | `Gallery` do mothership-ds ganha `itemsPerPage` (upstream, v1.7.0) — Sobre volta a usar o componente real (filtros, badges, cores por categoria, foto 4:3), paginado em vez da reimplementação das decisões #18/#21 | Feedback direto: "a galeria que foi colocada na landing page não se parece em nada com a galeria do design system... ficou brutalmente decepcionante". Causa raiz do desvio original: `Gallery` não paginava, então a seção foi reconstruída do zero em vez de a lib ganhar a capacidade que faltava — o mesmo erro de instinto já corrigido uma vez nesta sessão (decisão #12 já tinha esse princípio, de sempre resolver na lib quando o gap é dela). `.ms-gallery__grid` é `auto-fill` com colunas de no mínimo 220px (1 coluna só em telas estreitas, card real ~250-380px de altura) — `.portfolio-gallery` (`index.css`) fixa colunas por breakpoint (2 estreito / 3 a partir de sm / 5 quando largo E baixo, pra encolher o card na horizontal quando a altura for curta) e `.ms-gallery__desc` ganha `line-clamp: 2` (3 linhas era alto demais); mesmo assim, mais de 1 linha de cards nunca coube em nenhuma altura testada (nem 900px) — `useItemsPerPage` sempre mostra 1 linha, variando só quantas colunas nela | 2026-08-06 |
| 23 | Portfólio real (14 projetos, `public/portfolio/*.pdf`) — case study de 6 páginas por projeto, título/descrição extraídos do próprio PDF (não reescritos), capa (página 1) renderizada como thumbnail (`public/portfolio/thumbs/*.png`) | Conteúdo enviado pelo Valnez; texto e categorização (Digital Design pra landing pages/apps/web/dev, Brand Design pra identidade visual) já vêm organizados nos próprios PDFs — reaproveitar em vez de reescrever mantém a copy consistente com o material de apresentação que ele já usa em outros lugares | 2026-08-06 |
| 24 | `Gallery` do mothership-ds ganha `onClick` por item (upstream, v1.7.0) — clicar num projeto abre o PDF completo num `Modal`, mesma exibição de qualquer outro Modal do site (véu com blur, fecha no X ou clique fora) | Pedido direto: abrir o case "parecido com os modais", não uma rota nova ou visualizador próprio. `Gallery` não tinha nenhum jeito de saber que um item foi clicado — item vira `<button>` (`.ms-gallery__item-trigger`) só quando `onClick` está definido; sem a prop, continua só visual. `Modal` (`size="lg"`) com um `<iframe src={pdfUrl}>` dentro — sem visualizador de PDF próprio (ver decisão #26 sobre o porquê) | 2026-08-06 |
| 25 | Título do card da galeria (`.ms-gallery__title`) ganha `line-clamp: 2` + `min-height: 2.6em` (`index.css`, escopado a `.portfolio-gallery`) | Achado ao trocar os 9 projetos placeholder ("Projeto N", sempre 1 linha) pelos 14 reais: títulos como "Museu Mastodonte" e "Precificação Beauty" quebram pra 2 linhas, outros como "Hotel Fênix" cabem numa só — sem altura reservada uniforme, a altura do card (e por tabela, se a página estoura a viewport) passava a depender de qual título calhava de cair em qual página/coluna. `useItemsPerPage` (decisão #22) só continua válido pra **todas** as páginas se todo card tiver a mesma altura sempre, não só na média | 2026-08-06 |
| 26 | ~~PDF do case aberto num `<iframe src={pdfUrl}>` simples, sem visualizador próprio (ex.: pdf.js)~~ — **corrigido pela decisão #30**, ver lá | Mais simples, sem dependência de runtime nova, funciona com o visualizador nativo de PDF de qualquer navegador real (Chrome, Edge, Firefox, Safari) — que já abre links internos do PDF numa nova guia por padrão, satisfazendo o requisito sem código extra. Trade-off aceito conscientemente: o Playwright (ferramenta de teste deste projeto) desabilita de propósito o visualizador de PDF do Chromium que ele controla (PDF vira download, não renderiza no iframe) — não dá pra confirmar visualmente a renderização nem o comportamento de nova guia em automação, só manualmente no site publicado. Alternativa mais robusta (visualizador próprio com pdf.js, com controle garantido sobre o alvo de cada link) foi considerada e descartada por ora — mais dependência e trabalho pra um requisito que o navegador já cumpre por padrão. **Premissa quebrou na prática**: no Chrome Android, `<iframe>` não tem visualizador nativo de PDF nenhum — cai num fallback de "baixar o arquivo", tela preta | 2026-08-06 |
| 27 | `Modal` do PDF usa `size="full"` (upstream, mothership-ds) em vez de `size="lg"` | Feedback direto: `size="lg"` (880px) ficava pequeno demais pra ler o PDF confortavelmente. `full` é uma capacidade nova da própria lib (não um workaround local) — `width`/`height: 100%` explícitos, diferente de `sm`/`lg`/`md` que só têm `width` e encolhem em altura pro tamanho do próprio conteúdo. Como `.ms-modal__body` já é `flex: 1`, o `<iframe>` com `height: 100%` (antes `75vh`, um chute) passa a preencher o espaço de verdade | 2026-08-06 |
| 28 | PDF do case tem versão mobile própria (`public/portfolio-mobile/*-mobile.pdf`, 9 páginas em coluna única/retrato, vs. 6 páginas em `public/portfolio/*.pdf`) — servida no lugar da desktop quando a viewport tem menos de 640px de largura | Conteúdo enviado pelo Valnez, reformatado por ele pra leitura confortável em celular (sem zoom/scroll horizontal que a versão desktop, pensada pra tela larga, exigiria). Corte em 640px é o mesmo que o resto do arquivo já usa pra "estreito" (`useItemsPerPage`); decidido no instante em que o Modal abre (`pdfUrlFor`, função pura, não hook) — não precisa reagir a resize com o PDF já aberto | 2026-08-06 |
| 29 | ~~PDF do case trocou de `<iframe src={pdfUrl}>` (decisão #26) pra visualizador próprio (`src/components/PdfViewer.tsx`) — renderiza cada página em `<canvas>` via `pdfjs-dist`, com os links do PDF sobrepostos como `<a target="_blank" rel="noreferrer">` posicionados por cima do canvas na área exata de cada anotação de link~~ — **superado pela decisão #33**, ver lá | Bug real reportado: no Chrome Android o `<iframe>` não tem visualizador nativo de PDF — cai num fallback de "baixar o arquivo", tela preta, PDF nunca aparece (só no computador renderizava direto). Três opções foram apresentadas antes de qualquer código (pedido explícito): (A) botão "abrir externamente" no lugar do iframe, (B) embed via Google Docs Viewer, (C) visualizador próprio com pdf.js. Optado por C — mais completo e à prova de futuras correções de escopo (mesma renderização em qualquer navegador/dispositivo, sem depender de plugin nativo que a gente não controla nem consegue testar). `pdfjs-dist` foi instalado **só neste projeto** (`valnezJrLP/package.json`), não em mothership-ds — a lib anuncia explicitamente zero dependências de runtime além do React, e isso quebraria essa promessa pra todo consumidor dela por causa de um caso de uso só deste site. Efeito colateral positivo: resolve também a lacuna de teste da decisão #26 — o visualizador nativo do Chromium não dava pra confirmar em automação (Playwright desabilita de propósito), mas canvas/DOM sob controle do React é 100% testável; o clique num link do PDF abrindo genuinamente uma nova guia (não só o atributo `target="_blank"` presente) foi verificado via `context.waitForEvent("page")`, algo que nunca tinha sido provado antes nesta sessão, só assumido do comportamento padrão do navegador. **Substituído** quando Valnez passou a ter apresentações em HTML prontas pra cada case (decisão #33) — o pdf.js resolvia um problema (renderizar PDF em qualquer navegador) que deixou de existir quando o PDF parou de ser exibido em tela | 2026-08-07 |
| 30 | ~~`PdfViewer` (decisão #29) importado via `React.lazy()` + `<Suspense>` em `Sobre.tsx`, não como import estático no topo do arquivo~~ — **componente removido pela decisão #33**, ver lá | Achado proativo (usuário não reportou): import estático de `pdfjs-dist` inflou o bundle principal de 211KB pra 644KB (gzip 71KB→200KB) — peso baixado por toda visita ao site, mesmo por quem nunca abre um projeto do portfólio. `lazy()` isola `PdfViewer` (e o `pdfjs-dist` que ele importa) num chunk próprio (`PdfViewer-*.js`, ~428KB, gzip ~128KB), buscado só no clique real que abre o `Modal` — bundle principal volta a ~215KB (gzip ~72KB). `Suspense` com um `fallback` usando o `Loader` do mothership-ds cobre o instante entre o clique e o chunk chegar. O padrão de lazy-loading em si continua — só migrou pro `CaseViewer` (decisão #33), que é ainda mais leve por não depender de `pdfjs-dist` | 2026-08-07 |
| 31 | ~~Card da galeria do portfólio abaixo de `sm` vira lista horizontal (foto miniatura à esquerda, texto à direita) em vez do card padrão da `Gallery`~~ — **revertido pela decisão #32**, ver lá | Feedback direto: "no mobile eu preciso que a galeria fique em uma stack vertical de 2 projetos por vez... os tamanhos ficaram pequenos para a leitura". Tentativa inicial (só empilhar em 1 coluna, mantendo o card padrão foto-em-cima) não coube: photo sozinha ~240px de altura numa tela de 375px de largura, 2 cards davam >900px, e o espaço útil sem gerar scroll é ~547px nessa mesma viewport (`SectionShell` reserva `pt-24`/`pb-6`). Ideia do próprio Valnez ("stackar os dois verticalmente ao invés de stackar horizontalmente... aproveitamento maior de tela útil") interpretada errado como inverter o eixo do próprio card (`flex-direction: row`) — resultado visual não era mais "o card da Gallery", e foi rejeitado: "eu queria os cards no mesmo modelo que já estavam... sem inventar versão alternativa" | 2026-08-07 |
| 32 | Card do portfólio no mobile volta a ser o padrão da `Gallery` (foto 4:3 em cima, texto embaixo, sem alteração de layout) — 1 projeto por página (não 2); nas duas resoluções mais baixas testadas (360×640, 375×667), onde nem 1 card padrão cabe inteiro, só a grade (`​.ms-gallery__grid`) rola verticalmente por dentro — cabeçalho, pills de filtro e paginação continuam fixos, fora da área que rola | Pedido direto de reverter a decisão #31: manter o card exatamente igual ao resto do site, sem versão alternativa em "modo paisagem". Testado com o card padrão intocado: 2 por página não cabe em nenhuma das 4 resoluções mais estreitas; **1 por página também não cabe** em 360×640 e 375×667 (a foto sozinha do card já passa da altura útil disponível nessas duas telas) — só cabe inteiro a partir de 360×740. Confrontado com esse resultado medido, a escolha do próprio Valnez foi permitir rolagem, mas só dentro da área da galeria, não da página inteira — abre uma exceção pontual à regra de nunca criar scroll interno (decisão #18), restrita a este caso (card real maior que a tela mais baixa suportada) e sempre que o conteúdo já cabe sozinho (telas mais altas), o `overflow-y: auto` não mostra barra nenhuma — não é scroll sempre ligado, só quando o card genuinamente não cabe | 2026-08-07 |
| 33 | Clique num projeto do portfólio abre uma **apresentação em HTML** do case (`CaseViewer`, `src/components/CaseViewer.tsx`) em vez do PDF renderizado em `<canvas>` (decisões #29/#30, agora superadas) — o PDF vira **exclusivo pra download**, via botão já embutido no próprio HTML do case | Pedido direto de Valnez, que passou a gerar — com pipeline própria (`catalogo/`, fora do git, ver decisão #34) — uma versão HTML de cada case (fragmento `<article class="case">`, estilizado só com classes `ms-*` do mothership-ds + `case.css` de layout) pensada pra ser lida na tela, reservando o PDF pra quem quer levar o material embora. Elimina de quebra a dependência `pdfjs-dist` (~128KB gzip no chunk lazy) — o fragmento HTML de cada case pesa poucos KB. Os 14 fragmentos (`catalogo/workspace-portfolio/web/*.html`) foram copiados pra `public/portfolio-case/{slug}.html` com dois ajustes de path, feitos uma vez na cópia (não em runtime): `src='web/...'` → token `__CASE_IMG_BASE__` (as imagens do case foram junto pra `public/portfolio-case/img/`) e `href="pdf/....pdf"` → token `__CASE_PDF_HREF__`; `CaseViewer` troca os tokens pela URL real (`import.meta.env.BASE_URL` + `pdfUrlFor`, ambos extraídos de `Sobre.tsx` pra `src/lib/portfolio.ts` nesta mesma mudança, pra não duplicar a lógica entre os dois componentes) depois de buscar o texto (`fetch`) e injeta com `dangerouslySetInnerHTML` — conteúdo confiável (gerado pelo próprio pipeline do autor, não input de terceiro), sem superfície de XSS real a mitigar. `rel="noopener"` dos links externos do fragmento original virou `rel="noopener noreferrer"` na cópia, pra bater com a convenção já usada no resto do site (decisão #29). `.ms-modal__body` já rola sozinho (`overflow-y: auto`), então `CaseViewer` não precisa de nenhum controle de altura/scroll próprio — diferente do `PdfViewer`, que precisava preencher 100% da altura pra empilhar páginas de canvas | 2026-08-10 |
| 34 | Material de origem do pipeline de cases (`catalogo/` — três `.zip` enviados por Valnez: PDFs desktop/mobile novos + o workspace com `build.py`, `catalogo.json`, `imagens/` brutas e os HTMLs/JPGs de `web/`) fica **fora do git** (`.gitignore`), só os artefatos derivados (`public/portfolio*`, `public/portfolio-case/`) são versionados | `catalogo/workspace-portfolio` sozinho soma ~18MB (a maior parte em `imagens/`, capturas em alta resolução usadas só pra gerar os PDFs/HTMLs, não consumidas pelo site em si) — mesmo padrão que já valia pros PDFs (nunca existiu uma pasta "fonte" versionada ao lado de `public/portfolio/*.pdf`, só o resultado). Nada foi apagado da pasta original de Valnez; ela continua intacta em disco pra reprocessar (`python3 build.py --case 15-novo-projeto --web`, documentado no próprio `catalogo/workspace-portfolio/LEIA-ME.md`) quando entrar um 15º projeto — só não entra no repositório | 2026-08-10 |
| 35 | `Modal` do case ganha `className="case-modal"` (`Sobre.tsx`) — `.case-modal.ms-modal--full` (`index.css`) restringe a **largura** de volta pra `min(880px, 100%)` a partir de 1024px, mantendo `size="full"` (altura 100%, mobile sem mudança) | Pedido direto: com o case em HTML (decisão #33) em vez do PDF/canvas que justificou `size="full"` (decisão #27), a largura total da viewport em telas grandes deixava as linhas de texto do case compridas demais pra ler confortavelmente. 880px reaproveita o mesmo valor que `.ms-modal--lg` já usa pra "diálogo de texto comum" (ver JSDoc de `Modal.tsx` upstream) — não inventa medida nova. Resolvido com CSS local (`.case-modal.ms-modal--full`, duas classes — mais específico que a regra de uma classe só da lib, sem precisar de `!important` mesmo com `mothership-ds/styles.css` carregado depois de `index.css` em `main.tsx`), não mudança na lib: é um ajuste específico deste uso do Modal, sem motivo pra virar capacidade nova do `size` upstream. Só a largura muda — a altura `100%` de `size="full"` já funcionava bem pro case rolar por dentro (`.ms-modal__body`) e não tinha reclamação | 2026-08-11 |
| 36 | Fotos da seção "Galeria" dentro de cada case (`.case__galeria .ms-gallery__item`) perdem o `transform` de hover/active (`case.css`), mantendo sombra/brilho de borda | Bug reportado: bordas das fotos cortadas. Causa raiz medida ao vivo (Playwright, `boundingBox()` real com `.hover()`, não evento sintético — `:hover` não ativa por `dispatchEvent`): `.ms-gallery__item` (reaproveitado aqui só pelo visual — vidro, cantos arredondados) herda o hover de card **clicável** de uma `Gallery` de verdade (`scale(1.03) translateY(-4px)`), mas essas fotos não são clicáveis, só ilustram o texto do case. Sem nenhum respiro reservado, o crescimento de ~6px por borda ultrapassava `.ms-modal__body` (`overflow-x: hidden`, reserva da barra de rolagem) e cortava a foto mais próxima da margem. Primeira tentativa reservou espaço com `padding` + `margin` negativo (mesma receita de `.portfolio-gallery`, decisão anterior) — não resolveu: a margem negativa cancelava o padding, devolvendo a borda do item pro mesmo pixel de antes (medido, não só suposto). Como o hover não tem função aqui, a correção certa é não crescer: `transform: none` no hover/active desse contexto específico (`.case__galeria .ms-gallery__item:hover`), sem tocar no comportamento de hover de nenhuma `Gallery` clicável de verdade (portfólio da seção Sobre, decisão #22) | 2026-08-11 |
| 37 | Regra permanente + duas classes reutilizáveis pro "hover-lift cortado" (`.hover-lift-room`/`.hover-lift-off`, `src/index.css`, seção própria logo no topo do arquivo) — qualquer elemento com o hover-lift do mothership-ds (`.ms-gallery__item`, `.ms-hover-edge`, `translateY(-4px) scale(1.03)` no `:hover`/`:active`) dentro de um container que clipa precisa escolher uma das duas, documentado como checklist obrigatório antes de colocar esse hover num novo lugar | Pedido direto do Valnez, ao notar que o corte de borda já tinha acontecido duas vezes (decisão #22, galeria do portfólio; decisão #36, galeria dentro do case) — queria a causa raiz documentada e uma solução "por padrão", não outro remendo pontual. `.hover-lift-room` (padding-inline + overflow-x:hidden, `--hover-lift-gap` ajustável) cobre o caso clicável, reaproveitando a mesma estratégia da decisão #22; `.hover-lift-off` (`transform:none` no hover/active) cobre o caso decorativo da decisão #36 — a própria declaração de `.case__galeria .ms-gallery__item:hover` foi movida de `case.css` pra dentro do bloco de `.hover-lift-off` em `index.css` (um seletor a mais na mesma regra, não uma cópia), consolidando as duas correções que já existiam numa fonte única. A implementação de `.portfolio-gallery .ms-gallery__grid` (decisão #22) **não foi migrada** pra `.hover-lift-room` — tem nuance própria (padding assimétrico porque o hover só cresce pra cima, mais `overflow-y: auto` porque esse grid também rola de verdade) que a classe genérica não cobre; ficou só com um comentário cruzando pra esta decisão, como exemplo aplicado da regra. Documentado em três lugares pra reforçar (não só um): `index.css` (a implementação, com o aviso explícito sobre a armadilha de padding+margin-negativo se cancelarem — o erro real cometido a caminho da decisão #36), `docs/roteiro-prompts-landing-page.md` § Armadilhas conhecidas (item 5, mesmo formato das outras armadilhas já catalogadas ali) e este registro | 2026-08-11 |
| 38 | SEO/Open Graph completo (`index.html`): `title`/`meta description` reaproveitando a proposta de valor do `prd.md` §3 (texto já aprovado), `og:*`/`twitter:*` completos, `theme-color` (único hex hardcoded aceitável do projeto — meta tag é HTML estático, lido antes de qualquer CSS, não dá pra referenciar `var(--color-*)` ali; copiado do token real `--bg-base` do tema escuro, não inventado), `public/og-image.png` (1200×630, screenshot do Hero via Playwright, esperando o `.ms-hero__title` assentar antes de capturar) | Item da Etapa 8/Extras do roteiro, nunca feito — link do site compartilhado (WhatsApp, Instagram, LinkedIn) não mostrava preview nenhum antes disso. `og:url`/`og:image`/`twitter:image` usam a URL do preview de desenvolvimento (`valnezjr.github.io/valnezjrlp/`, decisão #8) por ser a única que existe — **precisam ser atualizadas junto** quando o domínio final for definido (item em aberto, fora do escopo de código). `og-image.png` é um screenshot real da página, não uma peça de design própria — trocar por uma arte dedicada (ex. com o styleguide do mothership-ds mais explorado) é melhoria válida pra depois, não bloqueia | 2026-08-11 |
| 39 | Duas faixas de `Marquee` (mothership-ds) em `SkillsMarquees.tsx`, abaixo da apresentação em Sobre.tsx: competências (`Badge` simples) e ferramentas (`Badge` com logo real + nome, `dangerouslySetInnerHTML` do SVG cru importado via `?raw`) | Pedido direto — competências fecha o campo `[PENDENTE]` do `prd.md` §5.3 desde a etapa 6; ferramentas é adição nova, fora do desenho original. `Marquee` é primitive pronta da lib (v1.2) sem nenhuma composição "LogoMarquee" ainda implementada lá — montar os `children` (Badge com/sem ícone) aqui é exatamente o previsto no comentário do próprio `marquee.tsx` upstream ("composições futuras... nenhuma precisa de estado ou lógica própria além de compor"). Logos baixadas **uma vez** e versionadas em `src/assets/tool-logos/` (svgo-otimizadas, ~35KB total), não buscadas de CDN em runtime — mesmo princípio da decisão #17 (sem dependência de rede externa a cada visita). Fontes por ferramenta: Figma/Illustrator/Photoshop/Canva/Inkscape/GIMP vêm do [devicon](https://github.com/devicons/devicon) (`-original`, coloridas, estilo "ícone de app"); CorelDRAW/Penpot vêm do [Simple Icons](https://simpleicons.org/) (marca monocromática — cor canônica dos dois é preto puro, então o `fill` do `<svg>` raiz foi trocado por `currentColor` na cópia local, herdando `var(--color-text)` do `Badge` e funcionando em tema claro/escuro sem precisar de dois hex fixos); Affinity Designer não existe em nenhum dos dois pacotes (nem Adobe nem Canva nem Affinity estão no Simple Icons — o próprio pacote não lista nenhum ícone Adobe, provável questão de marca registrada) — baixado do ícone real hospedado em Wikimedia Commons (`File:Affinity Designer V2 icon.svg`), uso informativo padrão de "ferramentas que uso" em portfólio, não redistribuição da marca como produto próprio. **Krita ficou de fora**: apareceu disponível no Simple Icons mas não é alternativa direta a nenhuma ferramenta que Valnez citou (não é editor vetorial nem geral de raster como os outros pares OSS) — incluir só "porque tinha" seria inventar conteúdo não pedido | 2026-08-12 |
| 40 | Seção Sobre ganha uma faixa de compactação por altura (`@media (max-height: 700px)` — mesmo limiar que `useItemsPerPage` já usa, não um segundo número mágico) reduzindo gaps a zero, `line-height`/`padding-block` do Badge das marquees e o tamanho do `Avatar` (48px → 32px, só nesse contexto) | As duas faixas de `Marquee` (decisão #39) estouravam ~19px em 1366×600 — a altura mais baixa já testada e documentada (decisão #7 e outras), onde a seção Sobre já não tinha nenhuma folga antes desta mudança. Medido com Playwright em cada etapa da compactação (não só suposto): tentativas por ordem — zerar gaps entre os blocos (~14px), `line-height:1` nos badges das marquees em vez de reduzir mais a fonte (fonte já estava no piso de legibilidade de 10px — trocar entrelinha em vez de tamanho de letra preserva a legibilidade, só remove espaço morto da caixa de texto), até sobrar ~19px reservados encolhendo só o `Avatar` (decorativo, iniciais "VJ" ainda placeholder) de 48px pra 32px — a peça que mais sobra fácil sem tocar em texto real. Resultado: ~3px de folga real em 1366×600 (testado), sem overflow em nenhum dos outros pontos da bateria padrão (360×640, 375×667, 1440×900). Em alturas normais (>700px) as marquees continuam no tamanho base (fonte 10px, ícone 14px) — a compactação extra só entra abaixo do limiar | 2026-08-12 |
| 41 | Correção pós-lançamento em `SkillsMarquees.tsx`: (a) gap entre as duas faixas de `Marquee` (2px→12px, respiro visual pedido pelo Valnez); (b) logo/nome "Affinity Designer" trocado por "Affinity" (`affinity-designer.svg` → `affinity.svg`); (c) todos os 9 SVGs de `src/assets/tool-logos/` reprocessados com `svgo --config` usando o plugin `prefixIds` (prefixo `tool-<nome-do-arquivo>__`) | (a) `.skills-marquees` usava `gap-0.5` (2px) do Tailwind — quase imperceptível; a regra `@media (max-height:700px) { .skills-marquees { gap:0 } }` da decisão #40 já zera esse gap nas alturas mais baixas, então subir o valor base não afeta o caso crítico de 1366×600 medido naquela decisão. (b) Valnez apontou que o ícone usado (azul, fundo navy, "Affinity Designer" separado — era o correto até a versão anterior) não é mais o atual: em 30/10/2025 a Serif/Canva lançou um rebrand que uniu Designer, Photo e Publisher num único app "Affinity" (freemium, dentro da Canva), com marca nova — "A" escuro (`#0e1318`) sobre fundo verde-limão (`#a7f175`). Novo arquivo baixado do Wikimedia Commons (`File:Affinity (App) Logo.svg`, Canva Inc., 30/10/2025, domínio público por não atingir o limiar de originalidade — mesmo tratamento informativo da decisão #39). Nome do badge passa de "Affinity Designer" pra só "Affinity", já que o app não separa mais personas por nome de produto. (c) Durante a investigação, descobri que os SVGs são injetados crus (`dangerouslySetInnerHTML`) na mesma página e o `Marquee` duplica cada badge pra loop contínuo — vários arquivos (gimp.svg e inkscape.svg, sourceados de bibliotecas diferentes) reusavam os mesmos IDs internos genéricos (`id="a"`, `id="b"`...) pra gradientes/`<use>`. Como `url(#id)`/`href="#id"` resolve pelo primeiro elemento com aquele ID **no documento inteiro** (não escopado por `<svg>`), a definição de um arquivo "vazava" pro outro — confirmado visualmente: o mascote do GIMP renderizava com parte da orelha sem preenchimento porque seu `fill="url(#a)"` resolvia pro `<path id="a">` do Inkscape (definido antes no DOM), não pro próprio gradiente. Corrigido rodando `svgo` com o plugin `prefixIds` (prefixo único por arquivo) em todos os 9 SVGs — mesmo princípio de isolamento da decisão #37 (bug de classe recorrente, corrigido na raiz em vez de arquivo por arquivo) | 2026-08-13 |
| 42 | Extras do roteiro: `SectionDots` (rail vertical de dots, borda direita, centralizado na altura da viewport) e `useWheelSectionNav` (troca de seção via scroll do mouse/trackpad, um passo por gesto) | Pedido direto. `SectionDots` **não** reaproveita o `StepIndicator` do mothership-ds (mesma classe que a `Gallery` paginada usa, decisões #18-22): aquele é horizontal por design (dot ativo cresce em LARGURA) e sequencial ("Etapa X de Y"); aqui a navegação é por seção nomeada e não-linear — CSS local (`.section-dots*`, `src/index.css`) com dot ativo crescendo em ALTURA, reaproveitando só os tokens de cor/token de tamanho (`var(--dot-size)`, `var(--color-text)` etc.), mesmo princípio da Navbar (decisão #5). Posição (`right: var(--space-2)`, centralizado verticalmente) validada com Playwright, não só suposta: `SectionShell` reserva `px-6` (24px) de cada lado do conteúdo real nas 4 seções, em qualquer largura de 360px a 1920px — o rail cabe dentro dessa margem em toda parte, sem precisar de breakpoint pra esconder. `useWheelSectionNav` (`src/lib/useWheelSectionNav.ts`) despacha no `<main>` (não em `document`/`window`) — como `Modal`/`Drawer` portam pra `document.body`, fora de `#root` (ver comentário de `.ms-page` em `index.css`), wheel dentro de um Modal aberto nunca chega no listener, sem precisar checar estado de modal explicitamente. Cooldown de 650ms por `ref` (não `state`, que não dá tempo de propagar antes do próximo evento de um gesto de trackpad, que emite dezenas de deltas pequenos) cobre a duração da transição de fade com folga — sem ele, um "swoosh" de trackpad trocava várias seções em sequência. Respeita scroll interno de verdade (`.portfolio-gallery .ms-gallery__grid`, decisão #32): sobe a árvore de `e.target` até achar um ancestral com `overflow-y: auto/scroll` e conteúdo de verdade pra rolar; se ainda houver espaço na direção do gesto, não faz nada (deixa o scroll nativo acontecer, sem `preventDefault`) — só troca de seção quando esse ancestral já está no limite (ou não existe nenhum). Verificado com `dispatchEvent` de `WheelEvent` real em vez de `page.mouse.wheel()` do Playwright: Chromium headless recusa entregar wheel sintético via CDP sem uma ativação real prévia da página (clique genuíno antes) — não afeta uso real (usuário físico sempre tem essa ativação), só a automação de teste | 2026-08-21 |
| 43 | `.sobre-content` (Sobre.tsx) ganha mais respiro entre a apresentação, as marquees e a galeria (`gap-1`→`gap-4`, 4px→16px), e passa a rolar internamente (`overflow-y: auto`) em vez de zerar o gap nas alturas mais baixas (`@media (max-height:700px)`, mesmo limiar da decisão #40) | Feedback direto: o gap entre `.skills-marquees` e seus vizinhos (`.sobre-intro` acima, `Gallery` abaixo) ficava apertado demais — só o gap *entre* as duas faixas de marquee (`.skills-marquees`, decisão #41) estava bom, sem mudança pedida aqui. Zerar `.sobre-content` inteiro nas alturas curtas (decisão #40) resolvia sem overflow, mas exatamente por isso nunca sobrava respiro nenhum ali — trade-off que passou a incomodar. Trocado por `gap: var(--space-2)` (8px, não mais 0) + scroll vertical como rede de segurança pro que ainda não couber, autorizado explicitamente ("mesmo que adicione um scroll vertical nas telas menores"). Medido com Playwright na bateria padrão (360×640 a 1920×1080): nas alturas normais (>700px) o gap de 16px cabe sem nenhum scroll, sem regressão. Nas alturas curtas e largas o suficiente pra `.sobre-content` não encolher via flexbox (ex. 1366×600, `sm:h-auto` da Tailwind precisou ser sobrescrito por `height:100%` dentro do mesmo media query pra o `overflow-y:auto` ter uma altura travada de verdade pra rolar contra), o scroll ativa de fato — confirmado revelando o paginador da `Gallery` (antes invisível, cortado pelo `overflow:hidden` do `SectionShell`) ao rolar `.sobre-content` até o fim. Em telas estreitas e curtas (ex. 360×640), o próprio flexbox (`min-h-0` já presente na `Gallery`) absorve o gap maior encolhendo a `Gallery`, que segue com seu próprio scroll interno (decisão #32) — `.sobre-content` não chega a precisar rolar nesses casos, sem overlap de duas barras de rolagem aninhadas | 2026-08-21 |
| 44 | Correções da auditoria (`.audit/`, 2026-08-21): (a) foco move pro `<h1>` da seção nova após navegação (`App.tsx`); (b) setas do teclado não trocam mais de seção com um Modal aberto; (c) `aria-controls` do hambúrguer só existe com o Drawer montado | (a) `a11y-001`: nenhum dos 4 caminhos de navegação (Navbar, `SectionDots`, teclado, wheel) movia foco ou avisava leitor de tela — quem usa leitor clicava e não tinha nenhum sinal de que mudou de seção. Centralizado em `App.tsx` (acha o primeiro `h1` dentro de `main` após `displayed` mudar) em vez de `tabIndex` por seção porque o `<h1>` de Home vem de dentro do `Hero` do mothership-ds, que não expõe ref/id pro elemento interno — cobre as 4 seções sem tocar na lib. Achado real ao verificar com Playwright (não só suposto): a implementação inicial usava um `useRef(true)` de "primeira renderização" que focava errado logo na carga da página — `<StrictMode>` (`main.tsx`) roda todo efeito 2x em dev sem cleanup, e um booleano simples marca "já rodei" na 1ª chamada e dispara o foco na 2ª simulada. Corrigido comparando o `displayed` anterior num ref (`prevDisplayedRef`) em vez de um booleano — imune ao double-invoke porque `displayed` não muda entre as duas chamadas sintéticas. (b) `a11y-002`: o `keydown` vive em `document` e só se abstinha com foco em campo de formulário — com o Modal aberto (que prende foco num `<button>`, que passa por esse guard) a seta trocava de seção "por baixo" do véu. Corrigido checando `document.querySelector('[role="dialog"]')` — confirmado que `Modal.tsx` desmonta de vez do DOM quando fechado (`if (!mounted \|\| !visible) return null`), então a checagem não deixa nenhum caso escapar, inclusive durante a animação de fechamento. `useWheelSectionNav` já era imune por outro motivo (escopado a `main`, que o Modal nunca atinge por portar pro `body`) — mesma proteção, caminho diferente. (c) `a11y-003`: `aria-controls` apontava pro id do `<nav>` de dentro do `Drawer`, que só entra no DOM com `open`; `aria-expanded` sozinho já carrega o estado | 2026-08-21 |
| 45 | `bundle-002` da auditoria (`.audit/`, 2026-08-21) — thumbs PNG do portfólio (2,1 MB, `public/portfolio/thumbs/`) — conversão pra WebP/AVIF fica **pendente no pipeline externo**, sem mudança de código aqui | O lugar certo pra gerar os thumbs já otimizados é o `catalogo/workspace-portfolio` (pipeline própria de Valnez, fora do repo — decisão #34), não uma conversão pontual aqui que ficaria dessincronizada do material fonte na próxima vez que um projeto for adicionado/reprocessado (`build.py --web`, `LEIA-ME.md` do próprio pipeline) | 2026-08-21 |
| 46 | `bundle-001` da auditoria (`.audit/`, 2026-08-21) — PurgeCSS sobre o CSS do mothership-ds — **dispensado** | ~4,5 KB gzip de economia (29% do CSS) não compensa o risco levantado na análise: `Badge`/`Modal` da lib montam classe modificadora via template literal (`` `ms-badge--${tone}` ``, `` `ms-modal--${size}` ``) — a string final nunca aparece completa como texto em lugar nenhum, então um PurgeCSS padrão as apagaria sem avisar. Não é hipotético: as cores de `Badge` já em uso neste projeto (`tone: "accent"/"violet"/"pink"/"success"/"orange"/"highlight"`, `Servicos.tsx`/`Contato.tsx`) caem exatamente nesse caso. Fazer certo exigiria levantar manualmente todo `tone`/`size`/`variant` de cada componente da lib usado aqui e manter esse safelist atualizado pra sempre, sem nenhum teste automatizado pra avisar se ele ficar desatualizado (o projeto não tem suíte de testes — auditoria § Limites) — custo permanente desproporcional ao ganho | 2026-08-21 |
