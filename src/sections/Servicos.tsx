import { SectionShell } from "@/components/SectionShell";

// Placeholder — conteúdo real na Etapa 5 (docs/prd.md §5.2).
export function Servicos() {
  return (
    <SectionShell>
      <h1 className="text-2xl font-semibold">Serviços</h1>
    </SectionShell>
  );
}
