import { SectionShell } from "@/components/SectionShell";

// Placeholder — conteúdo real na Etapa 6 (docs/prd.md §5.3). O
// portfólio desta seção busca projetos do GitHub dinamicamente (ver
// docs/architecture.md, decisão registrada), não a tabela estática do
// PRD.
export function Sobre() {
  return (
    <SectionShell>
      <h1 className="ms-h1">Sobre</h1>
    </SectionShell>
  );
}
