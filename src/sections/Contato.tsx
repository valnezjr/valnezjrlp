import { SectionShell } from "@/components/SectionShell";

// Placeholder — conteúdo real na Etapa 7 (docs/prd.md §5.4).
export function Contato() {
  return (
    <SectionShell>
      <h1 className="text-2xl font-semibold">Contato</h1>
    </SectionShell>
  );
}
