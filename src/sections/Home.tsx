import { SectionShell } from "@/components/SectionShell";

// Placeholder — conteúdo real na Etapa 4 (docs/prd.md §5.1).
export function Home() {
  return (
    <SectionShell>
      <h1 className="ms-h1">Home</h1>
    </SectionShell>
  );
}
